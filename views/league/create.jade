extends ../layout
block content
  h1 Create League 
  input(type='text' ng-model='student_name')
  //- div{{student_name}}
  //- div(ng-show="student_name == 'john'") You are the chosen one

  p= season.year 
  each contestant in season.contestants 
    a(href='/league')= contestant.name 
    img(src= contestant.image)
    
    
